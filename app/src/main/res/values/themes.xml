<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MinimalismPlanner" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MinimalismPlanner" parent="Base.Theme.MinimalismPlanner" />
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- ... -->
        <item name="android:actionBarStyle">@style/MyToolbar</item>
        <item name="actionBarStyle">@style/MyToolbar</item>
    </style>

    <style name="MyToolbar" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/backGrund</item>
        <item name="theme">@style/ThemeOverlay.AppCompat.ActionBar</item>
    </style>







</resources>